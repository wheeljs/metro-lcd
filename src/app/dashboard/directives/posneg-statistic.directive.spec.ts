import { PosnegStatisticDirective } from './posneg-statistic.directive';

describe('PosnegStatisticDirective', () => {
  it('should create an instance', () => {
    const directive = new PosnegStatisticDirective();
    expect(directive).toBeTruthy();
  });
});
